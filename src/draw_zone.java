import javax.swing.*;
import java.awt.*;

public class draw_zone extends JPanel{
    public draw_zone()
    {
        this.setPreferredSize(new Dimension(800,400));
        this.setBackground(new Color(0,0,0,70));
    }
}
